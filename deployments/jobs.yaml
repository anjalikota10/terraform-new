apiVersion: batch/v1
kind: Job
metadata:
   name: nginx-job
   namespace: dev
spec:
  template:
    spec:
      containers:
       - name: hello
         image: busybox
         command :  [echo, "this is k8s jobs"]
      restartPolicy: Never

